#!/usr/bin/python3
"""This function returns true  if the object is an instance or false"""


def is_same_class(obj, a_class):
    """Returns if object is exactly an instance or false"""
    return type(obj) == a_class
